[profile.default]
src = "src"
out = "out"
libs = ["lib"]

# Allow writing deployment info to parent directory
fs_permissions = [{ access = "read-write", path = "../" }]

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
